Do a pull and push of my code with a commit message according to the context of the change.

If when pull have a conflict, stop the command and ask the user to solve the conflict.

```bash
git pull
git add .
git commit -m "commit message"
git push
```